2023.206:
	- Update libmseed to 3.0.17, supports both miniSEED 2 and 3.
	- Remove -e (encoding) argument.
	- Remove -r (reclen) argument.
	- Remove the -M and -R options.
	- Add -m and -r options to match and reject patterns against the
	Source Identifier of each record, often following this pattern:
	`FDSN:<network>_<station>_<location>_<band>_<source>_<subsource>`.
	- Date-time format for -ts and -te options now autodetected, can be
	YYYY-MM-DD, or YYYY-jjj, or epoch formats.

2023.038: 0.4
	- Update libmseed to 2.19.8.
	- Add Markdown documentation.
	- Fix compiler warnings.

2013.274: 0.3
	- Update libmseed to 2.12 release.

2012.112: 0.2
	- Use sample time tolerance when trimming traces to specified time window.
	- Add -C option to compare time series and report where they are different,
	useful when diagnosing mismatch traces.

2012.070: 0.1
	- Initial version.
